services:
  - type: web
    name: kp-stocks-live
    env: python
    region: oregon
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python kp_server.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
